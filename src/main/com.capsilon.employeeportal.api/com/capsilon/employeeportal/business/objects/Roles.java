package com.capsilon.employeeportal.business.objects;

public enum Roles {

	HR, ADMIN, EMPLOYEE;
	
	//List<String> allowedESignatureProviders = Stream.of(EsignatureProvider.values()).map(Enum::name).collect(Collectors.toList());
}
